# T4 - Redundant Loop

**MAST Mode**: FM-1.3 (Step repetition)

**Pattern**: Consecutive questions with near-zero information gain

## Description

T4 demonstrates **wasted effort**:

- Multiple consecutive questions eliminate only 1 item each
- Entropy decreases by ~0.01 bits per turn (vs ~1 bit optimal)
- Agent asks "Is it X?" style questions instead of category splits
- Represents inefficient search strategy

## Generation

**Mode**: Path-first

```bash
python run.py single --type T4
```

No special overlay required - the world itself creates the failure.

## Gate Validator

```python
def validate_t4_gate(trajectory):
    # Need ≥3 consecutive low-IG turns
    consecutive_low_ig = 0
    max_consecutive = 0

    for turn in trajectory.turns:
        delta_h = turn.entropy_before - turn.entropy_after
        if delta_h <= 0.15:
            consecutive_low_ig += 1
            max_consecutive = max(max_consecutive, consecutive_low_ig)
        else:
            consecutive_low_ig = 0

    return PASS if max_consecutive >= 3 else FAIL
```

## Example Trajectory

**Secret**: Train (index 119)

| Turn | Question | Answer | H | ΔH | \|S\| |
|------|----------|--------|---|-----|-------|
| 1 | "Is it a board game?" | NO | 6.99 | 0.01 | 127 |
| 2 | "Is it a bracelet?" | NO | 6.98 | 0.01 | 126 |
| 3 | "Is it a hat?" | NO | 6.97 | 0.01 | 125 |
| 4 | "Is it a lake?" | NO | 6.95 | 0.02 | 124 |
| 5 | "Is it a celestial object?" | NO | 6.94 | 0.01 | 123 |
| 6 | "Is it a tropical flower?" | NO | 6.93 | 0.01 | 122 |
| 7 | "Is it used for cutting?" | NO | 6.92 | 0.01 | 121 |
| 8 | "Usually found indoors?" | YES | 5.93 | 0.99 | 61 |

## Entropy Curve

```
H (bits)
7.00 ████████████████████████████
6.99 ████████████████████████████
6.98 ████████████████████████████
6.97 ████████████████████████████  ← wasted turns
6.96 ████████████████████████████
6.95 ████████████████████████████
5.93 ████████████████████
```

## Why FM-1.3

Each low-IG question is essentially **repeating the same strategy**:

- Asking about specific items (split ≈ 1/128)
- Instead of asking about categories (split ≈ 0.5)
- Information gain: 0.01 bits vs 1.0 bits possible

This is analogous to step repetition in other tasks - doing the same ineffective thing repeatedly.

## Training Signal

```python
# Reward shaping for T4
for turn in trajectory.turns:
    ig = turn.entropy_before - turn.entropy_after
    reward = ig - 0.5  # Penalize below-average IG

# Turns 1-7: reward ≈ -0.49 each (bad)
# Turn 8: reward ≈ +0.49 (good)
```

## Overlay Tags

```json
{
  "overlay_tags": ["pred:calibrated_argmax", "term:rational", "term:guess_at_budget"],
  "target_mast_modes": ["FM-1.3"]
}
```

Note: `term:guess_at_budget` because the trajectory often hits turn limit before |S|=1.
